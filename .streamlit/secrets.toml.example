# Local: copy to secrets.toml (gitignored)
#   cp .streamlit/secrets.toml.example .streamlit/secrets.toml
#
# Streamlit Cloud: App settings → Secrets → paste the [auth] block below.

[auth]
username = "Foothills"
password = "Amish"
